//Write a function called capitalize that takes a string and returns that string with only the first letter capitalized. Make sure that it
//can take strings that are lowercase, uppercase or both.





const phrase = (text) => {

return phrase.charAt(0).toUpperCase() + phrase.slice(1)
}

console.log(string("hello"))